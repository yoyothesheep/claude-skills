# CONFIG — frontend-review
# Copy this file to CONFIG.md and fill in your values. CONFIG.md is gitignored.

DEV_URL=http://localhost:3000         # URL of the running dev server
PAGES=/,/about,/dashboard            # Comma-separated page paths to review
SCREENSHOT_DESKTOP=.claude/review-desktop.png  # Desktop screenshot (1440px), relative to project root
SCREENSHOT_MOBILE=.claude/review-mobile.png    # Mobile screenshot (375px), relative to project root
DESIGN_GUIDELINES_PATH=.claude/DESIGN-GUIDELINES.md  # Relative to project root
MAX_ITERATIONS=5                      # Max fix-screenshot cycles per page
